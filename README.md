# AM2 PACS — Login EXATO

Esta versão usa o painel esquerdo aprovado como uma única imagem (`left-panel-approved.jpg`).
Isso evita que `background-size: cover` recorte a tomografia e garante o mesmo enquadramento do mockup aprovado em desktop.

## Publicação
Substitua todos os arquivos do repositório por estes arquivos e publique no GitHub Pages.
Depois atualize com Ctrl+F5 ou Cmd+Shift+R.

## Integração
O formulário é visual. No `app.js`, substitua o `console.log` pela autenticação real do AM2 PACS.
